# HANDOVER — точка входа для новой сессии

Прочитай этот файл + `docs/FIX_JOURNAL.md` (хронология всех правок и багов по датам).
Память проекта (`memory/MEMORY.md`) загружается автоматически — там индекс решений.

Последнее обновление: **2026-07-02 (вечер)**. Ветка `main`, всё закоммичено и задеплоено
(Vercel автодеплой при push). Проверка перед коммитом: `cd apps/web && npx tsc --noEmit`
(+ `npm --workspace=@krest/web run build` для критичных правок).
Промпты старта/закрытия сессии Михаила — **`docs/SESSION_PROMPTS.md`**.

Полная хронология последней сессии (замок владельца, секьюрити-пачка, «Закрой день»,
сдачи в панели, scope-канон лидера, дайджест/метрики/журнал, CRON_SECRET, админы) —
**вверху `docs/FIX_JOURNAL.md`, раздел 2026-07-02 (с продолжением)**.

---

## ОТКРЫТЫЕ ЗАДАЧИ (продолжить отсюда)

### 1. СЛЕДУЮЩАЯ СЕССИЯ — раздел «Тренажёр» в приложении
По слову Михаила (02.07): работаем над `/m/trainer/*`. Контекст: тренажёр
местописаний (фаза 1 готова, фаза 2 = аудио+кружки — memory
`project_locations_trainer`), AI-чат «Учиться вместе с ИИ» (текстовый MVP готов,
фаза 2 = аудио/видео — memory `project_ai_chat_locations_trainer`), баннер
тренажёра врёт «одно из 5 ежедневных заданий» (канон: 4 практики, тренажёр —
по желанию; находка аудита 02.07, не чинилось). Задачу уточнит Михаил.

### 2. Бэклог UI (из аудита 02.07, не одобрялся — предлагать по готовности)
Мобильные карточки списка учеников (<860px — самый слабый экран), фильтры-чипы
(город/куратор/«без куратора»/блок), заявка→сразу город+куратор при одобрении
учеником (важнее после scope-канона: без куратора ученика не видит лидер),
«осталось сегодня» после сдачи практики, реакция «🙏 Аминь» в ленте, развести
2 стрика (заходы vs закрытые дни), блок «ожидают входа» (whitelist без /start),
отмена мисклика в выпадашке куратора, лендинг по брифу (устарел: «6 недель,
6 блоков, пасторы»), корзина вместо жёсткого удаления, лента куратора в панели
(notifications_log уже наполняется), drag-and-drop перепривязки, «Мой город» лидеру.

### Закрыто 02.07 (НЕ переделывать)
AI-чат course-leak (гард 404); HEIC (клиентская перекодировка, `heic-convert` не
нужен); вечерние напоминания для всех поясов (GH Actions hourly); CRON_SECRET
(был пустой → задан, кроны закрыты 401); секьюрити-пачка; «Закрой день» + сжатие
фото; AI-вердикты + «Сдачи» куратору; «Напомнить в Telegram»; колонка «Актив.»;
подтверждение удаления именем; дайджест куратору; метрики кураторов; /panel/audit;
/panel/admins (view-as под админа); аналитика запуска; чип «День N из 7»;
автовкладка 6-10; t.me-ссылки; scope-канон лидера (панель+RLS).

---

## КЛЮЧЕВОЙ КОНТЕКСТ

- **Дневная модель (КАНОН):** день закрыт = за ОДНУ локальную дату сданы ВСЕ 4 практики
  (фото креста + молитва + местописания-видео + пересказ-аудио); 7 закрытых дней → блок.
  Все 4 — ежедневные. Даты = `studentLocalToday` (пояс города, дефолт Бали). Эталон
  экранов — `api/m/cross-photo`. Подробности и частые баги — в `CLAUDE.md` (раздел
  «Дневные практики») и `memory/project_daily_day_model_canonical.md`.
- **ТАЙМЗОНЫ (канон, 2026-07-01):** любая per-student дата (сегодня/стрик/закрытие дня) —
  ТОЛЬКО через `studentLocalToday`/`localTodayStr` (`cities.timezone`), НЕ `baliToday()`/UTC.
  `computeActivity` требует `today` параметром. См. `memory/feedback_per_student_local_timezone.md`.
  Граница дня — строго полночь пояса города (сдвиг на 4:00 предлагали — Михаил ОТКЛОНИЛ).
- **Эталон фото креста:** Storage `block-resources/cross-reference/{order}.jpg`. Блок 1 (Малый
  Крест) = `1.jpg` (свой); блоки 2–10 = полный «Крест» (одинаковый `2..10.jpg`). ИИ качает по order.
- **Блок-гейт:** блок N открыт при ≥7 закрытых дней предыдущего блока (`is_block_unlocked`,
  чисто по прохождению) ИЛИ `can_skip_block_lock=true` (персонал). Проверено — багов нет.
- **Кто есть кто:** Михаил (sleezard@gmail.com) — super_admin, is_protected,
  единственный видит скрытых (`hidden_from_tracking`). Эля Ходус (@elyaforlife) — admin.
  Alex Magnier (@alex_magnier) — city_leader Москвы (city_id=10). Оля Мелешина
  (@Omeleshinka) — скрытый ученик (видит только владелец).
- **Замок владельца `owner_locked` (2026-07-02):** «Крест Капсула Тест» (@krest777237,
  Москва), «Миша Моряк» (@crosscapsule, Бали) и Денис Федосеев (@denhofesh) — менять
  может ТОЛЬКО владелец: гард `lib/admin/locked.ts` во всех мутирующих путях панели
  и бота, fail-closed. Снять/поставить: `UPDATE profiles SET owner_locked=...`.
- **Scope-канон лидера города (2026-07-02, решение Михаила):** лидер видит учеников
  ТОЛЬКО через кураторов своего города (цепочка людей); city_id самого ученика
  видимости НЕ даёт — ни в панели (`lib/admin/scope.ts`), ни в RLS (`is_visible_to`,
  миграция 20260702150000). Ученик без куратора виден только админам.
- **Кроны:** CRON_SECRET задан (был пустой!) — роуты отвечают 401 без него; Vercel
  (2 суточных слота: daily-reminder 12:00 UTC, silence-check 09:00 UTC + в нём же
  дайджест куратору) + GitHub Actions `hourly-reminders.yml` дёргает daily-reminder
  ежечасно (иначе 20:00-напоминания доходили только до Бали).
- **Сессия панели сверяется с БД на каждый запрос** (guard.ts, 2026-07-02): роль/город
  берутся свежими; `getPanelSessionFromReq` теперь async — НЕ забывать await в новых роутах.
- **Аудит ролей — триггер БД** `zz_log_role_change` (changed_by=NULL значит «вне панели»);
  привилегированные колонки profiles защищены от RLS-клиентов триггером `a_protect_privileged_columns`.
- **Прод-бот:** `@cross_notify_bot`. Токен ротирован 2026-06-30 (старый был утечкой в
  `js/config.js`, убран). Вебхук: `/api/telegram/webhook`; поставить из браузера —
  `GET /api/telegram/set-webhook` (super_admin или `?key=CRON_SECRET`).
- **SQL на проде** (без CLI): Supabase Management API,
  `POST https://api.supabase.com/v1/projects/aejhlmoydnhgedgfndql/database/query`,
  токен `sbp_...` из `.mcp.json`, тело строй через `jq -Rn --rawfile q FILE '{query:$q}'`.
- **Правила:** только русский; no `alert/confirm` (toast/inline); TS strict без `any`;
  миграции только через `supabase/migrations/`; коммит/деплой как обычно (git push в main).

---

## КАК ПРОДОЛЖИТЬ
Вставь в новую сессию prompt из ответа Михаилу (или: «Прочитай docs/HANDOVER.md и
docs/FIX_JOURNAL.md, продолжи с Фазы 4 ролей»).
