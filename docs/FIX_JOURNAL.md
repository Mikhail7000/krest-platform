# Журнал изменений и багов (КРЕСТ)

Хронология правок и найденных ошибок по датам — чтобы видеть историю и не повторять баги.
Новые записи добавляются **сверху**. Формат: дата · область · что было/причина · что изменил · коммит.

Связанные источники: **`docs/HANDOVER.md`** (открытые задачи + точка входа для новой сессии), `CLAUDE.md` (раздел «Дневные практики — частые баги»), `CHANGELOG.md` (релизы по версиям).

**ОТКРЫТО (на 2026-07-02):** AI-чат course-leak (латентно, закрыть до «10 писем»); бэклог улучшений панели/приложения (сводка в переписке 02.07 — лента куратора, «Напомнить в Telegram», аналитика запуска, мягкое удаление, /panel/audit и др.). HEIC — ЗАКРЫТ клиентской конвертацией (см. 2026-07-02, `heic-convert` не нужен). Детали — в `docs/HANDOVER.md`.

---

## 2026-07-02 — Замок владельца, секьюрити-пачка, «Закрой день», сдачи в панели

### «Часовой пояс: день обнулился в 23:31 МСК» — НЕ баг (кейс как у Веры)
Капсула Тест была в Бали-пуле (`city_id=1`, UTC+8) → день переключался в 19:00 МСК; фото
«01.07» загружено 30.06 22:09 МСК = 01.07 03:09 Бали — корректно. По решению Михаила
**Капсула Тест → Москва** (`city_id=10`, прямой SQL; whitelist чист, триггер не перетрёт).
«Миша Моряк» остался на Бали. Разбор Веры (Уфа UTC+5, телефон на МСК): даты штампуются
верно, последний заход 01.07 13:14 по Уфе, 5 закрытых дней подряд (27.06–01.07).

### 🔒 Замок владельца `owner_locked` (7577776) — Капсула Тест и Миша Моряк только под Михаилом
Миграция 20260701233000: `profiles.owner_locked` — менять такого пользователя (роль,
куратор, город, удаление, whitelist-слот) может ТОЛЬКО владелец (`is_protected`).
Карта из 46 путей мутации профиля (мульти-агентный аудит) → гард `lib/admin/locked.ts`
(`ownerLockBlocksIds`/`ownerLockedHandles`) во всех: панель (role/transfer/city/
curator-city/delete/add/attach/attach-curators) и бот (del_student, /addcurator,
/addleader, /add, /attach, /transfer). UI: 🔒 в списке, строка read-only не-владельцу.
Флаг выставлен обоим тест-аккаунтам (SQL). Попутно: transfer не проверял `is_protected` цели.

### 🔒 Секьюрити-пачка (7ef1e7c)
- **Удалены легаси-роуты без авторизации:** `/api/admin/church/register` (создавал
  role=admin кому угодно!), `/api/miniapp/streak` (писал по body.user_id), страница
  `/register-church` + ссылки с лендинга/middleware (B2B убран по v3.0).
- **Бот:** удалена линковка `/start <email>` — угон аккаунта по известному email
  (перезапись telegram_chat_id). Fallback-подсказка про email тоже заменена (кнопка MiniApp).
- **Сессия панели:** guard.ts сверяет роль+город с БД на КАЖДЫЙ запрос (все 16 вызовов
  getPanelSessionFromReq → await) — разжалованный теряет доступ сразу, не через 7 дней
  cookie; view-as проверяет роль актора по БД.
- **Удаление:** админа удаляет только супер-админ (панель+бот, симметрия с actions/role).
- **Миграция 20260702001000:** триггер `zz_log_role_change` — аудит ВСЕХ смен ролей
  (whitelist/бот/заявки/SQL шли мимо role_change_log; changed_by→nullable) + триггер
  `a_protect_privileged_columns` — RLS-клиентам (authenticated) запрещено менять
  привилегированные колонки profiles (легаси-политика «update own profile» без
  ограничения колонок позволяла из браузера сменить себе role).

### «Закрой день» на дашборде + сжатие фото (169b161, bf5c74e)
- **TodayCard**: 4 практики дня с переходом сразу на экран (было 2-3 перехода через урок);
  состояние «День закрыт». Данные из общего SWR-кэша block-status. SVG-иконки из
  `_components/icons` (как в Stage4Nav), эмодзи убраны (и ✨ в AiTrainerEntry → IconCards).
- **Сжатие фото креста на клиенте** (`lib/m/compress-image.ts`): ≤1600px JPEG перед
  отправкой — телефонные 3-10 МБ упирались в лимит тела Vercel ~4.5 МБ; **HEIC на iPhone
  перекодируется в JPEG** (Safari декодирует нативно) → серверный 415 остаётся бэкстопом,
  `heic-convert` НЕ нужен. EXIF: `imageOrientation: 'from-image'`.

### AI-вердикты и сдачи в панели куратора (6bc46c0)
- **Миграция 20260702003000**: `ai_matched/ai_feedback` в student_block_daily_cross —
  вердикт ИИ теперь персистится (терялся после ответа клиенту), upload пишет его.
- **Галерея фото**: маркеры «⚠️ не совпал» (бэкстоп) и «👁 без проверки ИИ» (fail-open:
  сбой ИИ/старый HEIC/тест) + фильтр «Без подтверждения ИИ», зум показывает комментарий.
- **Секция «Сдачи»** в карточке ученика: аудио-пересказы (плеер, оценка ИИ, транскрипт),
  местописания (стих, кружок/аудио, статус), эмоции — по блокам, ленивая загрузка,
  подписанные URL (роут `/api/panel/student/[id]/submissions`).
- **Единый `studentCardAllowed`** (lib/admin/scope): закрыта дыра — лидер города мог
  открыть ФОТО ученика чужого города (crosses-роут проверял только куратора); скрытых
  не видит и куратор (канон «только владелец»).
- **Канон «квиза нет»**: статус блока «Сдан» = ≥7 закрытых дней (как passed_blocks_all),
  колонка «Квиз» убрана — блоки с 7/7 не висят «В работе».

### Ревью-фиксы (адверсариальный воркфлоу по диффу дня; 8 кандидатов → 6 реальных)
- **HIGH — «текущий блок» навсегда Блок 1:** BlockList брал `cur` по `block_passed_at`,
  который в дневной модели НИКТО не пишет → после 7/7 Блока 1 TodayCard навсегда
  показывал ложное «День закрыт — все практики сданы» и не открывал практики Блока 2.
  Фикс: `cur` = первый блок с <7 закрытых дней, passed = блоки с ≥7 (канон); сабтайтл
  «все сданы» — только если сегодня реально сданы все 4. Выстрелило бы при первом же
  завершении блока.
- **MEDIUM — attach-curators без owner_locked-гарда** (обход замка через смену города) — добавлен.
- **MEDIUM — locked.ts fail-open при ошибке БД** — теперь fail-closed (error → блокируем).
- **LOW — fallback бота** советовал удалённый `/start email`; **LOW — мёртвый фильтр**
  «не совпали» (422 отклоняет до записи) → перепрофилирован в «без подтверждения ИИ»;
  **LOW — EXIF** на старых движках.

*Проверки: `npx tsc --noEmit` + `npm --workspace=@krest/web run build` — зелёные на каждом коммите. Миграции применены на прод через MCP.*

---

## 2026-07-01 — Запуск: лидеры городов (полный флоу), таймзоны, онбординг по ролям, эталон фото, данные

### Роль «лидер города» — доведена до полноценного флоу (иерархия super_admin→admin→city_leader→curator→student)
- **bf111af** `/addleader` в боте (сначала 1 ник) + раздельные кнопки «Добавить куратора / Добавить лидера» на стр. Кураторы (общий `AddStaffBar`).
- **a0c8749** Отдельная страница+навигация **«Лидеры городов»** (`/panel/leaders`, admin-only); смена роли **куратор→лидер** (`RoleModal` + `city_leader` в `actions/role` + синх whitelist); `/addleader` **мульти-ник** + выбор города кнопками (миграция `20260630210000_bot_pending_action_addleader`: `+payload`, CHECK `+= addleader/addleader_city`); счётчик «Лидеров городов» в Обзоре и в отчёте; общий `StaffAddForm`; Кураторы и Лидеры разнесены (стр. Кураторы — только кураторы/админы).
- **95ab3b7** Смена города у лидера (кнопка + `CityModal` + `api/panel/actions/city`, синх whitelist); статистика «Города» считает только города/страны **с учениками** (проходящие Крест, нули не в счёт); в строке куратора подпись **«👑 Лидер: <имя>»** (по городу).
- **101abd3** Двусторонняя привязка лидер↔куратор **через город**: на «Лидеры» — «🔗 Привязать кураторов» (пакетно, `api/panel/actions/attach-curators`); на «Кураторы» — «👑 Назначить лидера» (через `actions/city`).
- **42d3606** `/addcurator` доступен **лидеру города** в боте — кураторы в whitelist с его городом (`assigned_city_id`) + существующие профили переводятся в его город. `processAddNicks` принимает `assignedCityId`; pending-флоу тоже.
- **1738ed1** «Ожидающие входа» лидеры (в whitelist, ещё не `/start`) показываются в списке с пометкой **«⏳ ждёт первого входа»** + счётчик «+N ждут входа»; + мобильная дошлифовка `panel.css` (таблицы влезают по ширине телефона, ужаты отступы/кнопки/заголовки).
- **6938b0c** Скрытых учеников (`hidden_from_tracking`) видит **ТОЛЬКО владелец** в ЛЮБОМ scope — фикс утечки лидеру города (Оля Мелешина показывалась у Alex Magnier). Правка в `studentInScope` покрывает `/panel/students` и `/panel/activity`. + подпись роли лидера **«Лидер города <Город>»** в сайдбаре (`PanelNav`) и баннере view-as (город берётся в `(dash)/layout.tsx`).

### Онбординг по ролям (a135dfc, 60a4b2a)
Вход в приложение ветвится по роли (роль из `/api/miniapp/profile`, шаг привязки):
- **ученик** → «ваш куратор» (ник куратора → `curator-request`, как было);
- **куратор** → «ваш лидер города» (ник лидера → привязка по городу лидера, `api/m/leader-request`; ищет лидера в профилях ИЛИ в whitelist); можно **ПРОПУСТИТЬ** («У меня нет лидера») — куратора мог завести админ напрямую без лидера;
- **лидер города / админ** → без привязки, сразу в приложение. Свою панель лидер открывает через `/panel` в боте.

### ТАЙМЗОНЫ — класс бага «per-student дата по UTC/Бали вместо пояса города» (03aaedc, 219e449)
- **Симптом:** «🔥»-стрик в трекинге гас в 0 при реальном стрике (последняя закрытая дата = локальное «сегодня» ученика впереди UTC).
- **КАНОН (память `feedback_per_student_local_timezone`):** любая per-student дата — ТОЛЬКО через `studentLocalToday`/`localTodayStr` (`cities.timezone`, дефолт Бали), НЕ `baliToday()`/UTC. WRITE и READ одной величины — в одном поясе. `baliToday()` — только cron/оконные границы/админ-агрегаты.
- **Фикс:** `computeActivity(opened, worked, windowDays, today)` — `today` теперь ОБЯЗАТЕЛЕН (компилятор не даст забыть); `activity/ping`+`streak`, `curator/students` (+`cities.timezone`), `[id]/progress`, бот `/progress`, `trainer/[blockId]`+`complete` — все на локальную дату ученика. Дневные практики (`cross-photo/prayer/locations/recitation`) УЖЕ были на `studentLocalToday` — там багов нет (аудит-воркфлоу из 21 агента подтвердил).
- **Разбор кейса Веры (@Veronichka_pos) — НЕ баг:** пояс её аккаунта Уфа (UTC+5), а телефон на московском (UTC+3). Пересказ записан в 23:57 по её телефону = **01:57 по Уфе (уже 1 июля)** → корректно ушёл на 01.07 (доказано таймстампом в имени аудиофайла = client `Date.now()`, + скрин 23:58 = UTC+3). Даты штампуются верно. **Решение «сдвиг границы дня на 4:00» ПРЕДЛОЖЕНО и ОТКЛОНЕНО Михаилом** — оставляем строго полночь пояса города.

### Карточка трекинга (adb3849)
Убран дублирующий нижний ряд «ачивок» (`🔥 N дней подряд / 📘 Блок N / ✅ Сдал N бл.`) — повторял верхние мета-чипы (`Блок · Сдано · 🔥 дн. · Закрыто`). Оставлены только чипы (полная сводка без дублей).

### Блок-гейт проверен — БАГОВ НЕТ
`is_block_unlocked`: блок N открыт если у ПРЕДЫДУЩЕГО блока **≥7 закрытых дней** (пересечение 4 практик по дате) ИЛИ `can_skip_block_lock`. Чисто по прохождению, БЕЗ недельного таймера. Корреляция по всем ученикам чистая (никто не открыт «зря», никто с 7 днями не заблокирован). Приложение гейтит той же RPC (`block-gate.ts`); клиентское зеркало `block-completion.ts` совпадает по логике. «Сдано» = сданных блоков; «Закрыто» = закрытых дней (все 4 практики за дату).

### Данные / Storage (прямые изменения на проде, НЕ в git)
- **Эталон фото креста:** `block-resources/cross-reference/2.jpg..10.jpg` заменены на **ПОЛНЫЙ «Крест»** (блоки 2–10: «Принцип Сотворения»…«5 Уверенностей»); **`1.jpg` (Малый Крест) НЕ тронут**. ИИ-сверка `cross-photo/upload` качает `cross-reference/{order}.jpg`; ученик видит тот же файл на экране «Ежедневное фото». Заливка: Storage REST API (`POST /storage/v1/object/block-resources/…`) с ОБОИМИ заголовками `Authorization: Bearer` **И** `apikey` (ключ `sb_secret_…` из `apps/web/.env.local` — новый формат требует `apikey`!). Без деплоя.
- **Оля Мелешина** (`@Omeleshinka`, id `314c6920-cb80-471b-9869-770dfb1bd86e`): `can_skip_block_lock=false` (открыт только блок 1), `city_id=10` (Москва, Europe/Moscow), уже `hidden_from_tracking=true`, `curator_id=null`, `role=student` — проходит курс сама, видит только владелец.
- **Вера** (`@Veronichka_pos`): пересказ 30.06 перенесён `effective_date → 2026-06-30` (был 01.07) → 30.06 закрыт (grace по просьбе Михаила; сам по себе гейт сработал верно).
- **@denhofesh:** в whitelist (`testing_whitelist` id 55) как ученик, `assigned_curator_id=281ddca3-…` (Михаил). Ждёт `/start` в боте.

### Приветствие о запуске (Telegram-текст, НЕ код)
Переписано с иерархией Лидер→Куратор→Ученик; убран несуществующий «/чтототам»; ссылка ученику = бот **@cross_notify_bot** (не голый `presentation.html`). Финальный текст — в переписке сессии.

*Проверки на всех коммитах: `cd apps/web && npx tsc --noEmit` + `npm --workspace=@krest/web run build` — зелёные.*

---

## 2026-06-30

### Персонал → все блоки открыты + одобрение заявки как лидера/куратора города + добавление лидера на стр. Кураторы
- **Все блоки открыты персоналу (curator/city_leader/admin/super_admin):** миграция `20260630190000_staff_skip_block_lock` — BEFORE-триггер `z_staff_can_skip_block_lock` на `profiles` ставит `can_skip_block_lock=TRUE` для штатных ролей при любой записи (покрывает смену роли в панели, одобрение заявки, whitelist-триггер, прямой SQL) + разовый UPDATE существующих. Студентов не трогаем (тестировщик-ученик со своим TRUE сохраняется). Эффект: у персонала все 10 блоков открыты, видео без no-skip, квиз без лимита попыток — свободный просмотр/перепрохождение. RPC `is_block_unlocked` и клиент `BlockList` уже читали `can_skip_block_lock`. Раньше флаг авто-ставился только `super_admin`. *(Day-gate дневных практик `can_skip` не обходит — только `test_daily_accel`; для «листать блоки» это не нужно.)*
- **Одобрение заявки на доступ с выбором роли+города (веб-панель):** раньше заявку (`access_requests`) одобряли как ученик/куратор без города. Теперь admin/super_admin в `/panel/requests` жмёт **Учеником / 👤 Куратором (город опц.) / 👑 Лидером города (город обяз.) / Отклонить** — у куратора/лидера раскрывается выбор города. Миграция `20260630180000_access_request_leader_city` (CHECK `approved_role += city_leader`; колонка `approved_city_id → cities`). `decideAccessRequest` принимает `cityId`, пишет `approved_city_id`, зовёт `createTelegramProfile` с `role+cityId`; роль/город ставятся ПОСЛЕ триггера whitelist. Отложенное создание профиля (юзер ещё не открывал бота) при первом `/start` берёт `approved_role+approved_city_id` из заявки. Telegram-кнопки заявки оставлены простыми (ученик/куратор/отклонить) — город/лидер назначается в веб-панели (по просьбе Михаила). Файлы: `lib/admin/access-requests.ts`, `lib/telegram/ensure-profile.ts`, `api/panel/actions/access-request/route.ts`, `requests/RequestActions.tsx`+`page.tsx`. types.ts регенерирован.
- **Добавление куратора/лидера со страницы Кураторы:** инлайновый `AddCuratorBar` вынесен в `AddStaffBar.tsx` — для admin форма с выбором роли (Куратор/Лидер города) + город (лидеру обязателен); для city_leader — только куратор в его город. Раньше «Лидер города» был только в форме на стр. Ученики (Михаил не находил). `curators/page.tsx` грузит cities для админа.
- **Подтверждено:** заявки на доступ видит и admin (Эля), не только владелец — `/panel/requests` пускает admin+super_admin, данные не скоупятся; Telegram-уведомление о заявке (`notifyAdminsAboutRequest`→`getAdminChatIds`) уходит всем admin+super_admin.
- **Одобрение лидером города прямо из Telegram-кнопок бота:** к уведомлению о заявке добавлена кнопка «👑 Лидером города» → двухшаговый флоу: `leader_pick` показывает города кнопками (по 2 в ряд + «← Назад» `leader_back`), выбор города = `approve_leader:<reqId>:<cityId>` → профиль создаётся как `city_leader` с городом, заявка флипается с `approved_city_id`. Хелпер `editMessageReplyMarkup` в `lib/telegram/send.ts` (меняет кнопки, не трогая текст). callback_data ≤ 64 байт (uuid+cityId укладывается).
- **Фикс прав на inline-кнопки бота:** `handleCallbackQuery` авторизовал ТОЛЬКО `ADMIN_TELEGRAM_CHAT_IDS` (env-владелец) → Эля (admin) получала кнопки заявок, но нажать не могла. Теперь права = `getAdminChatIds` (super_admin+admin из БД, env-fallback) — как и рассылка уведомлений. `del_student` дополнительно защищён своим `getAdminProfile`.
- **Adversarial-ревью нашло и закрыло (5 находок):**
  - *(MEDIUM)* невалидный `cityId` (целое, но несуществующий город) ронял FK уже ПОСЛЕ создания профиля → полу-созданный аккаунт + заявка зависала в pending + ложное «уже обработана». Фикс: проверка существования города в `access-request/route.ts` ДО решения (чистая 400) + проверка ошибок update в `createTelegramProfile` (`ensure-profile.ts`) и на flip-update в `decideAccessRequest`.
  - *(MEDIUM)* демоушен персонал→student не сбрасывал `can_skip_block_lock` (экс-сотрудник оставался учеником с открытыми блоками/без no-skip/без лимита квиза). Фикс: `actions/role/route.ts` при демоушене из штатной роли в ученика ставит `can_skip_block_lock=false` (триггер ставит TRUE, но не снимает).
  - *(LOW)* гонка бот↔панель при одобрении заявки (бот обновлял `access_requests` без гарда `status='pending'` → возможен двойной нотифай/расхождение статусов). Фикс: условный флип `.eq('status','pending').select('id')` в вебхуке (approve и reject) — победитель ровно один.

### Роли «лидеры городов» — ФАЗА 4 (экран кураторов лидера + перепривязка + view-as для admin)
- **Экран «мои кураторы» для city_leader:** страница `/panel/curators` раньше была admin-only, теперь пускает и лидера города со scope по его городу (только кураторы его города + их ученики; добавление куратора доступно — роут сам относит его в город лидера). Управление (смена роли, массовая привязка) и view-as лидеру скрыты. Nav: пункт «Кураторы» теперь виден лидеру (`leaderVisible`). Файлы: `curators/page.tsx` (рефактор `loadCurators` под scope), `CuratorsView.tsx` (проп `canManage`), `PanelNav.tsx`.
- **Перепривязка кураторов по городам (admin-only):** доска «списками-колонками» — колонки=города (с именем лидера), карточки=кураторы, перенос выпадашкой «Переместить» (без drag-and-drop, надёжно на мобильном). Смена `profiles.city_id` куратора = переход под лидера нового города; ученики остаются за куратором (видимость нового лидера — через членство куратора в городе, `studentInScope`). Новые: `api/panel/actions/curator-city` (POST смена города, admin-only, только curator, не protected), `api/panel/curators/reassign` (GET данные доски), страница `curators/reassign/page.tsx` + клиент `ReassignBoard.tsx`; ссылка «⇄ Перепривязка по городам» на странице кураторов.
- **View-as для admin (Эли):** раньше view-as мог только владелец (super_admin+is_protected). Теперь admin может «войти как» curator/city_leader (НЕ как admin/super_admin — защита от эскалации, двойная проверка в `view-as/route.ts` и `guard.applyViewAs`); super_admin — как curator/city_leader/admin. В токен view-as прокидывается `tcity` (для наложения роли лидера). Кнопки view-as в `CuratorsView` показываются по роли цели и только реальному админу вне режима view-as. `ViewAsBanner` знает метку «лидер города».

### Фаза 4 — adversarial-ревью нашло пред-существующие дыры scoping лидера (закрыты)
Мульти-агентное ревью диффа (5 измерений × 2 скептика) подтвердило 5 находок (все 2/0), исправлены:
- **CRITICAL — `panel/enter/route.ts`:** вход лидера по ссылке из бота пересоздавал сессию БЕЗ `city` → постоянная cookie с `city=null` → `resolvePanelScope` давал `scopeCityId=null` → лидер видел учеников ВСЕХ городов (на `/panel`, `/panel/students`). Фикс: `signSession(... city: sess.city ?? null)`. (Sibling-роут `auth/telegram` city прокидывал корректно — отсюда и расхождение.)
- **HIGH — `lib/admin/scope.ts` `studentInScope`:** при scoped-роли без разрешённого scope (лидер с `city=null`) финальная ветка `return isOwner || !hidden` падала «открыто». Фикс: для не-админа без scope → `return false` (fail-closed). Затрагивало `students`, `activity/days`.
- **HIGH — `stats/stats-data.ts` `visibleStudents`/`getPanelStats`:** тот же fail-open в обзоре. Добавлен параметр `isScoped`: при `scopeCityId==null && isScoped` → `[]`; платформенные счётчики кураторов/админов — только при `!isScoped`. Оба вызова (`page.tsx`, `stats/route.ts`) передают `isScoped = !scope.isAdmin`.
- **MEDIUM — `actions/add/route.ts`:** лидер города мог прямым API привязать ученика к куратору ЧУЖОГО города (`body.curatorId` не проверялся). Фикс: если лидер задаёт `curatorId` — проверяем, что это куратор его города, иначе 403.
- **LOW — `curators/page.tsx`:** стат лидера переименован «Учеников в городе» → «Учеников у кураторов» (считается именно это; «в городе» расходилось со scope дашборда для учеников без куратора).
- **Заодно (defense-in-depth):** `api/panel/cities/route.ts` — гейт сменён с «не куратор» на `isAdminRole`, чтобы лидер не получал агрегаты по всем городам прямым вызовом (страница `/panel/cities` и nav и так admin-only).

### 🔒 БЕЗОПАСНОСТЬ — bot token ротирован и убран из репозитория (закрыто)

### 🔒 БЕЗОПАСНОСТЬ — bot token ротирован и убран из репозитория (закрыто)
- **Что сделали:** Михаил ротировал Telegram bot token в @BotFather (старый считается скомпрометированным), обновил `TELEGRAM_BOT_TOKEN` в Vercel, передеплой.
- **Чистка:** старый токен удалён из `js/config.js` (теперь `''` + коммент) и `docs/legacy/handoff.md` (заменён на `[removed-rotated-2026-06-30]`). Проверено: реального токена в трекаемых файлах больше нет. *(В git-истории старый — уже мёртвый — токен остаётся; полная зачистка истории не делалась.)*
- **Урок:** bot token НИКОГДА не в клиентском коде (`js/config.js` был утечкой). Только server-side env. Legacy `js/auth.js` слал sendMessage с клиента — плохой паттерн; токен пуст → вызов безопасно пропускается (`if (!TELEGRAM_BOT_TOKEN) return`).

### Вебхук слетел при ротации токена — восстановлен
- **Причина:** после revoke токена `getWebhookInfo` показал `url:""` — вебхук пустой, поэтому бот не отвечал на `/start`.
- **Фикс:** `setWebhook` новым токеном (`drop_pending_updates=true`). `TELEGRAM_WEBHOOK_SECRET` в Vercel НЕ задан → роут вебхука проверку секрета пропускает, secret_token не нужен.
- **Добавлено:** роут `GET /api/telegram/set-webhook` (коммит f1f5872) — ставит вебхук из браузера (super_admin-сессия или `?key=CRON_SECRET`), токен/секрет из env, чтобы впредь не возиться с curl.

### Новая иерархия: роль «лидер города» (city_leader) — ФАЗА 1
- **Иерархия:** super_admin (Михаил) → admin (Эля) → **city_leader** → curator → student. Лидер привязан к городу (`profiles.city_id`), один лидер на город.
- **Фаза 1 (БД, безопасно, готово):** миграция 20260630140000 — роль в `profiles_role_check`; `is_visible_to` обновлён (city_leader видит всех своего города — кураторов и учеников по city_id ученика ИЛИ по городу его куратора; куратор дополнительно видит своего city_leader). **Alex Magnier (@alex_magnier) super_admin → city_leader Москвы (city_id=10).** Эля Ходус → admin (см. выше).
- **Доступ city_leader к /panel пока ВЫКЛЮЧЕН** (не в allow-list авторизации) — включим в Фазе 2 вместе со scoping по городу, иначе лидер увидел бы всех.
- **Фаза 2 (панель — просмотр по городу, ГОТОВО):** `AdminSession.city` + хелперы `isAdminRole`/`isScopedRole`; auth (widget + бот `/panel`) пускает city_leader и кладёт city в токен; новый хелпер `lib/admin/scope.ts` (`resolvePanelScope`/`studentInScope`/`cityCuratorIds`) — единая видимость (куратор→свои, лидер→город, админ→все). Заскоуплены read-роуты: `students`, `activity/days`, `stats`(+stats-data), `student/[id]` (доступ к карточке/фото). Nav: лидер видит Обзор/Ученики/Активность; Заявки/Кураторы/Города — только admin (закрыто и для лидера). Обзор: заявки только админам; «статистика вашего охвата» для scoped-ролей. Заодно закрыта дырка: не-владелец-админ (Эля) больше не откроет карточку скрытого ученика по id.
- **Фаза 3 (добавление людей, ГОТОВО):** миграция 20260630160000 — `testing_whitelist.assigned_city_id` + триггер `apply_whitelist_role` теперь ставит при входе и город, и куратора (добавленные лидером попадают в его город/к нужному куратору). `add/route` — матрица прав: admin/super_admin → любого; лидер города → куратора+ученика (в свой город); куратор → ученика (сразу к себе). Лидеру обязателен город (для добавляемого лидера админ выбирает город). UI: ролевая add-форма в `StudentsClient` (селект роли по роли добавляющего + выбор города у админа), кнопка «+ Добавить» теперь у всех ролей. **Безопасность:** routes role/transfer/delete/access-request/attach переведены с «не куратор» на «только admin/super_admin» — лидер города НЕ может менять роли/удалять/решать заявки (мог бы из-за старого гейта). Строковые действия в таблице — только админу.
- **Фаза 4 (план, ещё не сделано):** отдельный экран «мои кураторы» для лидера; перепривязка кураторов между городами/лидерами (колонки-списки в панели admin); view-as для Эли (admin) как куратор/лидер (сейчас view-as только владелец, `tcity` в токене уже готов). Карта 28 точек — в разведке сессии.

### HEIC обходил ИИ-проверку креста + Эля Ходус → admin
- **HEIC-лазейка:** фото в HEIC (формат iPhone) ИИ-зрение не читает → раньше принималось БЕЗ проверки (`aiMatched=null` → не блокировался). Теперь роут `cross-photo/upload` **отклоняет** не-vision форматы (415 UNSUPPORTED_FORMAT) с просьбой прислать JPEG/PNG → мимо ИИ ничего не проходит. (Настоящая конвертация HEIC→JPEG требует npm-зависимости `heic-convert` — установку не разрешили; если одобрят, добавлю авто-конвертацию.)
- **Эля Ходус → admin** (прямой SQL): была student в Бали → теперь `role='admin'`, `curator_id=NULL`. Олю Мелешину (Оля `hidden_from_tracking=true`) не видит — скрытых видит только владелец. На текущий момент admin в системе один — Эля.

### ИИ засчитывал случайные фото вместо креста (палетка теней → ✅)
- **Причина:** роут `cross-photo/upload` намеренно НЕ блокировал при `matched=false` («мягко, записываем всегда» — раньше так сделали, чтобы 422 не резал настоящие кресты при баге «загрузка не регистрировалась»). Из-за этого любое фото проходило с ✅.
- **Фикс:** вернул блокировку явного «не креста» — `if (aiMatched === false) return 422 NOT_A_CROSS` (фото не сохраняется, день не закрывается). Блокирует ТОЛЬКО явный `false`; ошибка ИИ / HEIC / accel / сбой парсинга = fail-open (не режет настоящие). Промпт `check.ts` усилен: сфотографированный предмет/косметика/скриншот/селфи = `matched=false` всегда.
- **Плюс:** на экране фото — удаление фото ПОСЛЕДНЕГО дня (`/api/m/cross-photo/delete`, только самый свежий день, чтобы не «раскрыть» прошлый закрытый; inline-подтверждение).

### % курса показывал 0% при 5/7 закрытых днях блока 1
- **Причина:** `coursePct = сданныеБлоки / всего` (только блоки с `block_passed_at`). Пока идёт блок 1 (не сдан) → 0/10 = 0%, дни внутри блока не учитывались.
- **Фикс (`BlockList`, коммит f7c36cc):** `pct = (passed + min(closedDaysТекущегоБлока/7, 1)) / total`. 5/7 дней блока 1 → ~7%. Не баг данных — формула была «только сданные блоки».

### Лента — кружок не отправлялся («Не удалось отправить») на iPhone
- **Причина (главная):** на iOS mime записи = `video/mp4;codecs=avc1`, а бакет `community-media` в `allowed_mime_types` разрешает строго `video/mp4` → Supabase отклонял upload. Роут публикации брал `file.type` как есть, не отбрасывая `;codecs`.
- **Фикс:** в `api/m/community/post` mime чистится `.split(';')[0]` (как в роуте местописаний).
- **Сопутствующее:** битрейт видео-кружка снижен (360к видео + 48к аудио ≈ 3 МБ/60с) — раньше после фикса зависания полные 60с при 500к давали ~4+ МБ и риск упереться в лимит тела Vercel 4.5 МБ; + клиентский гард с понятным текстом, если запись всё же >4.3 МБ. Те же настройки (fps-кап, `start(1000)`, битрейт) продублированы в видеокружок местописаний (`LocationCard`).

### Рейтинг — каскадная анимация, ярче переливы, своя картинка-фон
- **Анимация появления** карточек по порядку (1 место → 2 → 3 …) через `lb-reveal` + задержка по позиции (`--lb-i`); переливы ярче, свечение сильнее. Коммит 37ba68a.
- **Своя картинка на карточку** (решение Михаила: фон всей карточки + затемнение, настройка в профиле): миграция `leaderboard_bg_path` (бакет `avatars`); роут `/api/m/leaderboard-bg` (GET/POST/remove, ≤5MB jpeg/png/webp); рейтинг отдаёт `bg_url`; `LeaderboardCard` рисует фон-слой + scrim + светлый текст (`lb-card--has-bg`), tier-перелив остаётся рамкой/свечением; в профиле раздел «Моя карточка в рейтинге» (`LeaderboardCardSettings`) с предпросмотром и загрузкой/удалением.

---

## 2026-06-29

### 🔒 БЕЗОПАСНОСТЬ — найдена утечка (ожидает решения Михаила)
- **Что:** реальный Telegram **bot token захардкожен** в клиентских файлах легаси-MiniApp и в доке:
  `js/config.js`, `apps/web/public/miniapp/js/config.js` (последний раздаётся публично по `/miniapp/js/config.js`!), `docs/legacy/handoff.md`.
- **Причина:** легаси vanilla-MiniApp клал bot token прямо в клиентский конфиг (так нельзя — токен только server-side).
- **Хорошо:** `.env`, `.env*.local`, `.mcp.json` корректно в `.gitignore`; anon-key в клиенте — это нормально (публичный, защищён RLS); `scripts/populate-content.mjs` берёт ключ из `process.env`; в `docs/*.md` service_role в основном плейсхолдеры `eyJ...`.
- **Действие:** РОТИРОВАТЬ токен в BotFather (считать скомпрометированным) → затем убрать из файлов. Статус: ждёт подтверждения.

### Запись кружка в ленте — зависало видео (аудио продолжалось)
- **Область:** `apps/web/src/app/m/trainer/[blockId]/useRecorder.ts` (используется лентой/community и тренажёром).
- **Причина:** видео-энкодер MediaRecorder стопорился на ~10-15с (частый баг iOS Safari при высоком fps и записи без timeslice): видео «вставало», аудио шло дальше.
- **Фикс:** `recorder.start(1000)` (сброс чанков раз в секунду) + `frameRate: { ideal: 24, max: 30 }`. Запись и так была 60с со счётчиком «Xс / 60с».
- **Коммит:** _(см. ниже, общий с рейтингом)_

### Рейтинг (трекинг) — склонение «очков» + оформление мест
- **Область:** `apps/web/src/app/m/tracking/*` + `api/m/tracking/route.ts`.
- **Было:** «3 очков» (всегда родительный падеж); места только gold/silver/bronze(4-10).
- **Фикс:** правильное склонение (1 очко / 2-4 очка / 5+ очков, по последней цифре). Новые tier-уровни: 1 золото, 2 серебро, 3 бронза, **4-6 небесно-голубой**, **7-10 зелёный**, остальные нейтральные — с переливами (анимация `lb-flow`) и свечением (`lb-glow`), + `prefers-reduced-motion`.
- **Коммит:** _этой даты_

---

## 2026-06-28

### Экран «Молитва» — нельзя набрать 7 закрытых дней (тот же баг, что у фото)
- **Область:** `api/m/prayer/[blockId]/route.ts`, `m/prayer/[blockId]/PrayerClient.tsx`.
- **Причина:** список капился на 7 слотов (`while days.length < 7`) и счётчик показывал «молитва-дни», а не закрытые дни. При частичных днях (помолился, день не закрыт) до 7 закрытых не добраться.
- **Фикс:** `futureCount = max(0, 7 - closedDays - 1)`; счётчик = `closed_days`; статус по дню через `is_day_closed` (день закрыт / «молитва есть · день не закрыт» / сегодня). Доказательство: тест-аккаунт молитва 6 ≠ закрытых 3.
- **Коммит:** 3794163

---

## 2026-06-27

### Местописания и пересказ — сделаны ежедневными (сброс по дням)
- **Причина:** экраны были одноразовыми («сдано когда-либо») → со 2-го дня не пересдать → день не закрыть. А модель требует все 4 практики каждый день.
- **Фикс:** статус «за сегодня» по `effective_date == studentLocalToday`; счётчик закрытых дней. Коммит 0cd7d27.

### SVG-иконки вместо эмодзи (местописания) + комментарий пересказа только за сегодня
- Эмодзи ✅/🎤/✓ → `IconCheck`/`IconMic`; комментарий AI не висит с прошлого дня. Коммит 2f969f8.

### «Учиться вместе с ИИ» — накопительный квиз по всем открытым блокам
- Было: квиз по стихам одного блока. Стало: смысл (конспект `summary_md`) + стихи всех открытых блоков, приоритет текущему. Коммит 6309477.
- **Известный баг (не исправлен):** при невалидном/чужом blockId `courseId=null` → в промпт попадают блоки всех курсов. Латентно (курс один), закрыть до запуска «10 писем».

### Города — убраны Находка и Владимир
- Удалены из таблицы `cities` (coming_soon, 0 учеников) миграцией с защитой. Коммит 0cfe4d4.

---

## 2026-06-26 — 2026-06-27

### Дневная модель — откат к «все 4 практики в один день» (пересечение)
- Пробовали независимый счёт по практикам (min из 4) → Михаил откатил. Итог: закрытый день = дата со всеми 4 практиками; 7 дней → блок. Миграция 20260626120000, коммит 79acac2.

### Экран фото креста — согласован с «Закрыто дней» + снят лимит слотов
- Счётчик = closed_days (не «фото-дни»); статус по дням; «не закрыт» только для прошлых дат; будущих слотов `max(0, 7 - closedDays - 1)` (до 7 закрытых всегда дойти). Коммиты fbc1a58, 721e390, 300b31b.
